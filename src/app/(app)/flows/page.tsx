import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function FlowsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Flow Builder</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Email flow builder content goes here.</p>
      </CardContent>
    </Card>
  );
}
