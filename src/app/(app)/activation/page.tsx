import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function ActivationPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Activation</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Activation dashboard content goes here.</p>
      </CardContent>
    </Card>
  );
}
