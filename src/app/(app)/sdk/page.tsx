import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function SdkPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>SDK Setup</CardTitle>
      </CardHeader>
      <CardContent>
        <p>SDK setup guide and documentation goes here.</p>
      </CardContent>
    </Card>
  );
}
